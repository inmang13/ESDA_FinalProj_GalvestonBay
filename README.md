1. Created a folder called GalvestonBay in the Docker.
2. Navidate to the path in terminal. I think the follwoing commands will work.
   
git init \n
git remote add origin https://github.com/inmang13/ESDA_FinalProj_GalvestonBay.git \n
git pull origin master
